/**
 * Graph - Kruskal
 */

module.exports = {
  /**
   * Return the weight of the minimum spanning tree
   * @param Object (graph representation) - { a:[['b',2],['c',3]], b:[['a',3]], c:[[]]}
   * @return int
   */
  weight: null,

  /**
   * Return the vertices in the minimum spanning tree
   * @param Object (graph representation) - { a:[['b',2],['c',3]], b:[['a',3]], c:[[]]}
   * @return Array<T>
   */
  mst: null
};