/**
 * Graph - Breadth First Search
 */

module.exports = {
  /**
   * Determine if two vertices are connected
   * @param Object (graph representation) - { a:[['a',2],['c',3]], b:[['a',3]], c:[[]]}
   * @return bool
   */
  connected: null,

  /**
   * Return the vertices in the search path if they are connected
   * @param Object (graph representation) - { a:[['a',2],['c',3]], b:[['a',3]], c:[[]]}
   * @return Array<T>
   */
  search: null
};