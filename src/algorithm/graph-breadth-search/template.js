/**
 * Graph - Breadth First Search
 */

module.exports = {
  /**
   * Return the vertices in the search path if they are connected
   * @param Object (graph representation) - { a:['c','b'], b:[['a']], c:[['a]]}
   * @return Array<T>
   */
  search: null
};